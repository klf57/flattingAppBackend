/**
 * Sends queries to the database and returns the results from the dbs reaction.
 */

const {dbQuery} = require('./QueryHandler');

exports.getChores = async function(userId){

    let result = 'something';

    return result;
}